---
import Container from "../../../components/ui/Container.astro";
import PageMasthead from "../../../components/ui/PageMasthead.astro";
import ProseContent from "../../editorial/components/ProseContent.astro";

interface Props {
  title: string;
  description: string;
  eyebrow: string;
  intro: string;
  donationLabel: string;
  donationUrl: string;
  sourceUrl: string;
}

const { title, eyebrow, intro, donationLabel, donationUrl, sourceUrl } =
  Astro.props;
---

<section>
  <PageMasthead {eyebrow} {title} description={intro} />

  <Container class="pt-12 pb-20 sm:pt-16 sm:pb-28">
    <div
      class="grid items-start gap-10 lg:grid-cols-[minmax(0,1fr)_20rem] lg:gap-14"
    >
      <div class="min-w-0">
        <ProseContent>
          <slot />
        </ProseContent>

        <p class="mt-10 max-w-3xl text-sm wrap-break-word text-ambedkar-900/70">
          Originally published at{" "}
          <a
            class="font-semibold text-ambedkar-700 underline underline-offset-4 hover:text-ambedkar-900"
            href={sourceUrl}
          >
            {sourceUrl.replace(/^https?:\/\//, "").replace(/\/$/, "")}
          </a>
          .
        </p>
      </div>

      <aside
        class="on-dark card h-auto bg-ambedkar-950 p-6 text-white sm:p-8 lg:sticky lg:top-24"
        aria-label="Donation link"
      >
        <p class="eyebrow eyebrow-light">Make a contribution</p>
        <h2
          class="mt-4 font-display text-2xl leading-[1.12] font-semibold tracking-title"
        >
          Support AKSC's work
        </h2>
        <p class="mt-3 leading-7 text-white/75">
          If your employer offers matching gifts, donate through its company
          portal. Otherwise, use AKSC's direct donation link.
        </p>
        <a class="btn btn-primary mt-6" href={donationUrl}>
          {donationLabel}
        </a>
      </aside>
    </div>
  </Container>
</section>
